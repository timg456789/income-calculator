/*var test = require('tape');


function(err, data) {
    t.ok(!err, err);
    t.ok(data);
}


function Book() {

    this.enter = function () {

    }

    this.getBalance = function () {
        return 123;
    }
}

var book = new Book();
book.enter(10000);
book.enter(10000);
book.enter(-10000);

test('aggregates', function(t) {
    t.plan(1);
    t.equals(book.getBalance(), 20000);
});*/